// Fase I §3.3: los tipos de BD se generan, nunca se escriben a mano. Este
// archivo solo reexporta el tipo real generado en @aquila/shared (D-11) —
// @nuxtjs/supabase espera encontrarlo en esta ruta por convención.
export type { Database } from '@aquila/shared'
