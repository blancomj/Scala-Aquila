{
  "name": "aquila",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "tsc --noEmit -p tsconfig.json && pnpm -r typecheck",
    "lint": "eslint . && pnpm --filter @aquila/web lint",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "verify": "pnpm build && pnpm typecheck && pnpm lint && pnpm test",
    "db:push": "node scripts/db-push.mjs",
    "db:push:dry": "node scripts/db-push.mjs --dry-run",
    "db:push:prod": "node scripts/db-push.mjs --prod",
    "db:push:prod:dry": "node scripts/db-push.mjs --prod --dry-run",
    "db:types": "node scripts/db-types.mjs",
    "db:types:prod": "node scripts/db-types.mjs --prod",
    "dev:login": "node scripts/dev-login.mjs",
    "test:edge": "deno test --no-check --coverage=coverage/edge supabase/functions/_shared/ && deno coverage coverage/edge --lcov --output=coverage/edge/lcov.info && node scripts/check-edge-coverage.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@aquila/financial-kernel": "workspace:*",
    "@aquila/liquidation-engine": "workspace:*",
    "@aquila/shared": "workspace:*"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@playwright/test": "^1.62.1",
    "@supabase/supabase-js": "^2.112.3",
    "@types/node": "^22.10.2",
    "@types/pg": "^8.21.0",
    "@vitejs/plugin-vue": "^5.2.4",
    "@vitest/coverage-v8": "^2.1.8",
    "@vue/test-utils": "^2.5.0",
    "dotenv": "^17.4.2",
    "eslint": "^9.17.0",
    "happy-dom": "^15.11.7",
    "pg": "^8.23.0",
    "prettier": "^3.4.2",
    "supabase": "^2.114.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1",
    "vitest": "^2.1.8",
    "vue": "3.5.41"
  }
}
